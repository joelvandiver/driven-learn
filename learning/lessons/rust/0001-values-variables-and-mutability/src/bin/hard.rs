// Hard — "Immutable by design"      run with: cargo run --bin hard
//
// Compute the average of three scores WITHOUT using `mut` on the value you
// report. The final value you print must live in an IMMUTABLE binding.
//
// - Scores to use: 88, 92, 79.
// - Print exactly: `average is 86` (truncating integer division is fine).
// - You may use `mut` internally only if you can justify why — aim to avoid it.
// - Stretch (optional): add `const MIN_SCORES: usize = 1;` and use it in an
//   `assert!` documenting your assumption before computing.
//
// Acceptance: compiles on stable; prints `average is 86`. Be ready to explain,
// when grading, why each binding is or isn't mutable.

fn main() {
    // your code here
}
